<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_static_manager"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.tiny.demo.firstlinecode.staticmanager.StaticManagerActivity">

    <Button
        android:id="@+id/btn_online"
        style="@style/btn_style_lower"
        android:text="online"/>

    <Button
        android:id="@+id/btn_offline"
        style="@style/btn_style_lower"
        android:text="offline"/>

    <Button
        android:id="@+id/btn_online_field"
        style="@style/btn_style_lower"
        android:text="online field"/>

    <Button
        android:id="@+id/btn_offline_field"
        style="@style/btn_style_lower"
        android:text="offline field"/>

    <TextView
        android:id="@+id/txt_login_url"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="--"/>

</LinearLayout>
